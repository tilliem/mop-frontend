const defaultState = {
  'petitions': {}, // keys are petition slugs, values are petition objects
  'signatureStatus': {} // keys are petition IDs, values are strings
};

export default defaultState;
